{
  "name": "poolcontrol",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "prisma generate && cd client && npm install && npm run build",
    "postinstall": "prisma generate || true",
    "seed": "node api/seed.js"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.7.0",
    "@prisma/client": "^7",
    "@types/pg": "^8.20.0",
    "bcryptjs": "^3",
    "date-fns": "^4",
    "dotenv": "^17.4.2",
    "jsonwebtoken": "^9",
    "pg": "^8.20.0",
    "prisma": "^7",
    "pusher": "^5.2.0"
  }
}
